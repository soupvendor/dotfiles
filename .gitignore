.DS_Store
*.local

# Machine-local mise config — git identity vars, and anywhere else you want a
# setting to stay on one machine. install.sh generates mise.local.toml.
.mise.*.local.toml
.mise.local.toml
.mise/*.local.toml
mise.*.local.toml
mise.local.toml
mise/*.local.toml
