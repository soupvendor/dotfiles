// Zed settings.
//
// Docs: https://zed.dev/docs/configuring-zed
// `zed: open default settings` in the command palette shows everything
// unset here.
//
// Unlike vscode/, settings.json IS tracked for Zed: nothing rewrites it
// behind your back, so it does not churn from machine to machine.
//
// The "Cell Tower" theme referenced below is not from the extension
// registry — it is zed/themes/cell-tower.json in this repo, symlinked to
// ~/.config/zed/themes/. Zed loads every JSON file in that directory and
// hot-reloads on save.
{
  "edit_predictions": {
    "provider": "zed"
  },
  "telemetry": {
    "diagnostics": true,
    "metrics": false,
    "anthropic_retention": false
  },
  "minimap": {
    "show": "always"
  },
  "agent_servers": {
    "claude-acp": {
      "type": "registry"
    }
  },
  "icon_theme": {
    "mode": "system",
    "light": "Zed (Default)",
    "dark": "Zed (Default)"
  },
  "base_keymap": "VSCode",
  "ui_font_size": 16,
  "buffer_font_size": 15,
  // Installed by the bootstrap — a cask on macOS, the release zip in
  // mise.linux.toml's final hook. Same face alacritty and tmux use.
  "buffer_font_family": "JetBrainsMono Nerd Font",
  // "system" makes Zed follow the desktop rather than pick for itself: it
  // subscribes to the XDG portal's org.freedesktop.appearance color-scheme and
  // repaints when it changes. Plasma flips that at sunset (see plasma/ and the
  // AutomaticLookAndFeel key), so both entries below are live and nothing here
  // needs a timer of its own.
  "theme": {
    "mode": "system",
    "light": "Cell Tower",
    "dark": "Cell Tower Night"
  }
}
